/** Brain input/output layout — keep in sync with World.buildInputs() and Cell.act(). */
export const BRAIN_TOPOLOGY = { inputs: 12, hidden: 8, outputs: 2 };
export const DIET_COLORS = {
    herbivore: '#5ad46a',
    carnivore: '#ff5c5c',
    omnivore: '#f5a623',
};
