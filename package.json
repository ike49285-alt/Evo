{
  "name": "evo",
  "version": "0.1.0",
  "private": true,
  "description": "Evo — a stable virtual ecosystem where virtunisms grow and adapt",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch",
    "serve": "http-server . -c-1 -p 8080",
    "build:artifact": "node tools/build-artifact.mjs"
  }
}
