{
  "name": "evo",
  "version": "0.1.0",
  "private": true,
  "description": "Evo — a cell evolution sandbox inspired by Cell Lab: Evolution Simulator",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch",
    "serve": "http-server . -c-1 -p 8080"
  }
}
